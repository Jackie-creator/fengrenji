# sandbox

一个**独立的新项目**，暂时寄放在本仓库中。

## 说明

- 与本仓库的 LeatherPattern AI（皮具裁剪图纸生成器，见根目录 `README.md` 与 `backend/`）**没有关系**，不共享代码、依赖与构建流程。
- 目录名 `sandbox` 只是占位，确定项目名后可以直接改名。
- 技术栈尚未确定，等内容明确后再补脚手架。
- 将来可能整个迁出到独立仓库，因此保持自包含：所有文件都放在本目录下，不要往仓库根目录或 `backend/` 里加东西。

## 现状

空项目，仅有本 README 占位。

## 待定

- [ ] 项目名与一句话定位
- [ ] 技术栈与目录结构
- [ ] 初始化脚手架（依赖、入口、测试）
